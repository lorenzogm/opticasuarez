# #383 Disable legacy React Router deployment workflows

Disable `deploy-production.yml` and `deploy-preview.yml` which deploy the old React Router app. The new `web-vercel-deploy.yml` handles apps/web deployment.
